<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- LinearLayout horizontal para imagen, nombre, apellido y botón de llamada -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="5dp">

        <!-- ImageView para la imagen del contacto -->
        <ImageView
            android:id="@+id/contactImageView"
            android:layout_width="83dp"
            android:layout_height="59dp"
            android:layout_marginRight="5dp"
            android:src="@drawable/contact_image" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/nombreTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Nombre"
                android:textSize="18sp"
                android:textStyle="bold"
                android:layout_marginLeft="5dp"
                android:textColor="@android:color/black" />

            <TextView
                android:id="@+id/apellidoTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Apellido"
                android:textSize="18sp"
                android:textColor="@android:color/black"
                android:textStyle="bold"
                android:gravity="center_vertical" />

            <TextView
                android:id="@+id/telefonoTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Teléfono"
                android:textSize="18sp"
                android:textColor="@android:color/darker_gray"
                android:paddingTop="8dp" />
        </LinearLayout>

        <!-- Botón para simular una llamada -->
        <ImageView
            android:id="@+id/callButton"
            android:layout_width="109dp"
            android:layout_height="66dp"
            android:layout_marginLeft="5dp"
            android:src="@drawable/sym_call" />
    </LinearLayout>

</LinearLayout>
